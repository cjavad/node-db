
{
    "username":"admin",
    "password":"password",
    "port":3434,
    "#note":[
        "#when linking to this file it is relative to the app.js",
        "#so place the .conf file in the same dir as the app.js",
        "#and use it as so node ./path/to/app/app.js someconfigfile.conf",
        "#NOTE: this file will not be selected automatically"
    ]
}
